import pandas as pd
database = pd.DataFrame([1,2,3])
class recommend:
    def __init__():
        content = []
    def callApi(userdata):
        jdata = jsonify(userdata)
        api_creds = []
        response = call(jdata,creds = api_cred)
    def getRecommendations(user_id):
        userdata = database['user_id']
        response = callApi(userdata)
        return response
    